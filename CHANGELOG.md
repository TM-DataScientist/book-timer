# CHANGE LOG

## 2026-05-11

- 開始日と開始時刻を現在時刻に更新するボタンを追加し、開始日の変更に合わせて終了日も既存ルールどおり同期するようにしました。

## 2026-05-08

- 開始日欄をTabなどで抜けたとき、終了日を開始日の変更日数ぶん自動調整して、同日終了や複数日セッションの期間を保てるようにしました。
- 開始日・開始時刻、終了日・終了時刻をそれぞれ同じ行に配置し、Tabキーで日時入力を自然な順番に移動できるようにしました。
- 読書セッションの入力を開始日/開始時刻と終了日/終了時刻に分け、日付をまたぐ読書時間を直接指定できるようにしました。
- 開始日を変更したとき、終了日が未入力または変更前の開始日と同じ場合は終了日の入力値も開始日に合わせるようにしました。
- 既存の保存済み `session_date` から開始日/終了日を復元できるようにしました。
- Googleカレンダー登録の共通処理を分離し、汎用イベント登録用のヘルパーを追加しました。
- 読了履歴Notebookがリポジトリ直下と`notebooks/`配下のどちらから実行しても履歴ファイルを見つけられるようにしました。

## 2026-04-29

- 同じ本の読了間隔チェックが現在時刻ではなく読了日同士を比較し、未来日登録でも前後1ヶ月未満を誤登録として止めることをテストで明確化しました。
- 同じ本の読了登録は前回読了日から1ヶ月以上空いていない場合に誤登録として止めるようにしました。
- 読了リストの同一日付・同一タイトルの重複登録を防止し、同名タイトルの別日登録は再読として確認してから追加するようにしました。
- 読了履歴から累計・年別・月別の冊数を集計し、アプリ画面に簡易バー付きで表示するようにしました。

## 2026-04-26

- 開始時刻 `22:00`、終了時刻 `25:00` のように、24 時以降の時刻を翌日の時刻として入力できるよう改善
- 24 時以降の時刻入力に対する回帰テストを追加

## 2026-04-05

- 読了履歴を `data/reading_history.parquet` で管理し、既存の `.book_timer_state.json` から自動移行できるよう改善
- `読了リストに追加` ボタンを追加し、入力中の書名と日付から読了履歴を登録できるよう改善
- Notebook から読了履歴を確認できる `notebooks/reading_history.ipynb` を追加
- 読了履歴の Parquet 保存、JSON 移行、保存順序を検証する回帰テストを追加

## 2026-04-04

- Google カレンダー登録時に失効した `token.json` から自動で再認証へフォールバックするよう改善
- Google カレンダー登録をバックグラウンド実行に変更し、認証中や API 呼び出し中に Tkinter 画面が固まらないよう改善
- Google Calendar API 呼び出しの例外処理を補強し、UI に失敗メッセージを返せるよう改善
- Google 認証キャッシュと登録処理の回帰テストを追加
